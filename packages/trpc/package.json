{
  "name": "@hako/trpc",
  "version": "0.0.1",
  "private": true,
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "dotenv -e ../../apps/api/.env.test -- vitest run",
    "test:watch": "dotenv -e ../../apps/api/.env.test -- vitest",
    "test:coverage": "dotenv -e ../../apps/api/.env.test -- vitest run --coverage"
  },
  "dependencies": {
    "@hako/db": "workspace:*",
    "@hako/types": "workspace:*",
    "@trpc/server": "^11.0.0",
    "rate-limiter-flexible": "^5.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@hako/config": "workspace:*",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^4.1.0",
    "dotenv-cli": "^11.0.0",
    "typescript": "^5.5.0",
    "vitest": "^4.1.0"
  }
}
